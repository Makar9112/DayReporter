# Контекст проекта DayReporter

> Файл для продолжения работы в новом чате Cursor.
> Дата актуализации: 2026-07-15.

## Суть

Streamlit-приложение для анализа журнала заявок СПбМТСБ (нефтепродукты):
статистика, графики Plotly, проверка 6 критериев недобросовестных практик,
лимит заявок на инструмент, HTML-отчёт.

Репозиторий: https://github.com/Makar9112/DayReporter  
Ветка: `main`  
Деплой: Streamlit Community Cloud, entrypoint `app.py`.

## Структура

```
DayReporter/
├── app.py              # UI Streamlit + session_state для удержания файла
├── analytics.py        # сводка, графики Plotly
├── criteria.py         # критерии №1–№6, burst-диагностика
├── utils.py            # парсинг Excel/времени, базис F/J, фильтр сессии
├── requirements.txt
├── runtime.txt         # python-3.11.9 для Cloud
├── .streamlit/config.toml
├── .devcontainer/      # добавлено на GitHub пользователем
├── README.md
├── CONTEXT.md          # этот файл
└── .cursorignore
```

Язык интерфейса и комментариев: **русский**.  
Ответы ассистенту: **на русском**.

## Ключевое поведение

### Загрузка Excel
- Обязательные колонки: Номер заявки, Время фиксации заявки, Код инструмента, Направл., Цена, Статус.
- Время с миллисекундами: `ЧЧ:ММ:СС.мс`.
- Базис: код на `J` → ОТП вагон; на `F` → вагон без ОТП; иначе по названию.
- Валидация байтов: пустой/все нули/не ZIP → понятная ошибка (не «File is not a zip file»).

### Фильтр сессии (по умолчанию ВКЛ)
- Интервал **11:00–13:00** — отсекает пакет «Корзины» около **10:45**.
- Настраивается в сайдбаре.

### Критерии
| № | Суть | Примечание |
|---|------|------------|
| 1 | ≥3 исполненных покупок за ≤1 с | упрощённо |
| 2 | ≥7 улучшений цены без сделки за ≤1 с | упрощённо |
| 3 | ≥7 покупок за ≤1 с | корзина = исключение, факт всё равно в тексте |
| 4 | ≥3 покупок за ≤100 мс | то же |
| 5 | верхняя граница + объём/доля | то же; объём ≈ сумма заявок |
| 6 | >500 покупок по инструменту за день | по умолчанию 500 |

Чекбокс «Корзина» по умолчанию **выключен**.

### Session state
Данные файла хранятся в `st.session_state` (`df_all`, `upload_name`, …), чтобы после
«Скачать отчёт» анализ не сбрасывался. Есть кнопка «Очистить загруженные данные».

### Лимит на инструмент
По умолчанию **250** заявок на код инструмента за день.

## Известные проблемы данных пользователя

Локальные выгрузки на Desktop (папка торговых отчётов):
- Файлы за **01.07, 02.07, 03.07** и часть других — **повреждены** (файл .xlsx из одних `0x00`, не ZIP).
- Нормальные примеры: **09.07**, **14.07** (начинаются с `PK`).
- Лечение: пересохранить в Excel как .xlsx или выгрузить журнал заново из терминала.

Путь к примерам (кодировка путей Windows может «ломаться» в консоли):
`Desktop\…торговые отчёты\{ДД.ММ}\Журнал заявок_*.xlsx`

## Что уже сделано в git (важные коммиты)

- Initial Streamlit Cloud setup
- Fix: session persistence after download
- Clearer error for corrupt/empty xlsx
- Пользователь добавил Dev Container Folder на remote

## Чего намеренно нет

- Telegram-бот: **отменён**, файл `report_images.py` удалён.
- Секреты / API-ключи не используются.

## Как воспроизводить локально

```bash
pip install -r requirements.txt
streamlit run app.py
```

Логи Cloud: share.streamlit.io → приложение → Manage app → Logs.
Ассистент **не** видит Cloud-логи сам — нужен текст/скрин от пользователя.

## Предпочтения пользователя (из правил)

- Отвечать по-русски, кратко.
- Коммиты только по запросу; push в `main` — с осторожностью/одобрением.
- Не коммитить `.xlsx`, `.env`, secrets.
- Не создавать лишние markdown без нужды (CONTEXT.md — по явной просьбе).

## Возможные следующие задачи

- Доработать критерии под уточнённую методику биржи.
- Поддержка `.xls` / HTML-выгрузок.
- UX Cloud (размер файлов, подсказки про битые xlsx).
- Тесты на реальных журналах 09.07 / 14.07.
